```erlang
sign(N) when is_number(N) ->
	if
		N > 0 -> positive;
		N < 0 -> negative;
		true -> zero
	end.

sign(N) when is_number(N) ->
	case dummy of
		_ when N > 0 -> positive;
		_ when N < 0 -> negative;
		_ when true -> zero
	end.  
```


